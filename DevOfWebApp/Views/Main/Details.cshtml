@model DevOfWebApp.Models.Entities.Преподаватели

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Преподаватели</h4>
    <hr />
    @await Component.InvokeAsync("ПреподавателиDetails", Model)
</div>
<div>
    @if (User.IsInRole("Admin"))
    {
        <a asp-action="Edit" asp-route-id="@Model?.IdПреподавателя">Edit</a> @("|")
    } 
    <a asp-action="Index">Back to List</a>
</div>
